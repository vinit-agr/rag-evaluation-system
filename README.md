# RAG Evaluation System

A Python framework for evaluating retrieval-augmented generation (RAG) pipelines with
chunk-level and token-level (character span) metrics.

See `implementation_plan.md` for detailed steps and `brainstorm.md` for architecture context.
